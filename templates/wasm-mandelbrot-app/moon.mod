name = "mooncraft/wasm_mandelbrot_app"

version = "0.1.0"

import {
  "moonbitlang/async@0.18.1",
}

readme = "README.md"

repository = ""

license = "Apache-2.0"

keywords = [ "mooncraft", "wasm", "template" ]

description = "MoonCraft template with WebAssembly frontend compute."

preferred_target = "native"
